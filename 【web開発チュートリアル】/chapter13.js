const chapter13 = [
    { id: "13-1", title: "通知ダイアログ：alert()", content: "ユーザーに「保存しました！」などのメッセージをポップアップで知らせるには <b>alert()</b> を使います。<br><br>右のコードを実行（またはボタンをクリック）して、アラート画面を出してみましょう！", initialCode: "<button onclick=\"showAlert()\">通知を出す</button>\n\n<script>\n    function showAlert() {\n        alert(\"これは通知ダイアログです！\");\n    }\n</script>" },
    { id: "13-2", title: "確認ダイアログ：confirm()", content: "「本当に削除しますか？」のように「はい/いいえ」を尋ねるには <b>confirm()</b> を使います。OKを押すと <code>true</code>、キャンセルだと <code>false</code> になります。<br><br>右のボタンを押し、if文と連動する動きを確認しましょう！", initialCode: "<button onclick=\"checkDelete()\">データを削除する</button>\n<p id=\"status\"></p>\n\n<script>\n    function checkDelete() {\n        // confirmの結果を変数に入れる\n        let result = confirm(\"本当に削除してもよろしいですか？\");\n        \n        if (result === true) {\n            document.getElementById(\"status\").innerText = \"削除しました。\";\n        } else {\n            document.getElementById(\"status\").innerText = \"キャンセルしました。\";\n        }\n    }\n</script>" },
    { id: "13-3", title: "入力ダイアログ：prompt()", content: "ユーザーから文字を入力してもらうポップアップを出すには <b>prompt()</b> を使います。<br><br>右のボタンを押して、あなたの名前を入力してみてください。画面に反映されます！", initialCode: "<button onclick=\"askName()\">名前を入力する</button>\n<p id=\"welcome-msg\"></p>\n\n<script>\n    function askName() {\n        // promptで入力された文字を変数に入れる\n        let userName = prompt(\"あなたの名前を教えてください\");\n        \n        if (userName != null && userName != \"\") {\n            document.getElementById(\"welcome-msg\").innerText = userName + \"さん、ようこそ！\";\n        }\n    }\n</script>" },
    { id: "13-4", title: "【超重要】ブラウザにデータを保存：LocalStorage", content: "ブラウザを閉じても消えないデータを保存するには <b>localStorage（ローカルストレージ）</b> を使います！<br><br><b>・保存する時：</b> <code>localStorage.setItem(\"鍵の名前\", データ);</code><br><b>・取り出す時：</b> <code>localStorage.getItem(\"鍵の名前\");</code><br><br>右のコードで、入力したメモを保存し、一度コードを消して書き直しても「取り出す」ボタンで復活するか試してみましょう！", initialCode: "<input type=\"text\" id=\"memo\" placeholder=\"メモを入力\">\n<button onclick=\"saveData()\">保存する</button>\n<button onclick=\"loadData()\">取り出す</button>\n<p id=\"output\"></p>\n\n<script>\n    function saveData() {\n        let memoText = document.getElementById(\"memo\").value;\n        // 「myMemo」という鍵の名前で保存！ブラウザを閉じても消えません\n        localStorage.setItem(\"myMemo\", memoText);\n        alert(\"保存しました！\");\n    }\n\n    function loadData() {\n        // 「myMemo」という鍵の名前でデータを取り出す\n        let savedText = localStorage.getItem(\"myMemo\");\n        document.getElementById(\"output\").innerText = \"保存されていたメモ：\" + savedText;\n    }\n</script>" },
    { id: "13-5", title: "第13章のまとめテスト！", content: "ダイアログとLocalStorageを組み合わせて、「訪問者の名前をずっと覚えておく機能」を完成させましょう！<br><br>【課題】<br>1. 画面が読み込まれたら <code>localStorage.getItem(\"visitorName\")</code> で名前を探す。<br>2. なければ <code>prompt()</code> で名前を聞き、<code>localStorage.setItem()</code> で保存するコードを完成させてください！", initialCode: "<h2 id=\"display-name\"></h2>\n\n<script>\n    // 1. 保存された名前を取り出す\n    let savedName = localStorage.getItem(\"visitorName\");\n    \n    // 2. もし名前が保存されていなかったら（初回アクセス時）\n    if (savedName == null) {\n        // ここにpromptを使って名前を聞く処理を書く\n        let newName = prompt(\"初めてですね！お名前は？\");\n        \n        // ここにlocalStorageで新しい名前を保存する処理を書く\n        localStorage.setItem(\"visitorName\", newName);\n        \n        savedName = newName;\n    }\n    \n    // 画面に表示する\n    document.getElementById(\"display-name\").innerText = \"こんにちは、\" + savedName + \"さん！\";\n</script>" }
];